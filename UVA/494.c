#include<stdio.h>
#include<string.h>
#include<stdlib.h>

int main(void) {

  char *input = (char *)malloc(sizeof(char) * 512);
  unsigned int counter = 0, i, length;
  while(scanf("%[^\n]s") == 1) {
    length = strlen(input);
    for(i = 0; i < length; i++) {

    }
  }
  return 0;
}
